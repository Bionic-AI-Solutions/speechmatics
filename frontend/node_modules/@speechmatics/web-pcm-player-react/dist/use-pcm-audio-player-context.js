import { useContext } from 'react';
import { pcmPlayerContext } from './context.js';

function usePCMAudioPlayerContext() {
  const value = useContext(pcmPlayerContext);
  if (!value) {
    throw new Error("Must call this hook in child of <PCMPlayerProvider>>");
  }
  return value;
}

export { usePCMAudioPlayerContext };
//# sourceMappingURL=use-pcm-audio-player-context.js.map
